/*
 * ESP32 Configuration Template
 * 
 * Copy this file to config.h and update with your actual credentials.
 * The config.h file is excluded from git to protect your credentials.
 */

#ifndef CONFIG_H
#define CONFIG_H

// WiFi Configuration - Update with your network details
#define WIFI_SSID "Your-WiFi-SSID"
#define WIFI_PASSWORD "Your-WiFi-Password"

// MQTT Broker Configuration - Update with your broker details
#define MQTT_BROKER_IP "192.168.1.XXX"
#define MQTT_BROKER_URI "mqtt://192.168.1.XXX:1883"
#define MQTT_PORT 1883

// MQTT Topics (usually don't need to change these)
#define MQTT_TOPIC "boilerstat/reading"
#define MQTT_CONTROL_TOPIC "boilerstat/control"

// Timing Configuration
#define PUBLISH_INTERVAL_MS (5 * 1000)  // 5 seconds - adjust as needed

// Device Configuration
#define MQTT_CLIENT_ID "esp32-boilerstat"

// GPIO Pin Assignments (update for your ESP32 wiring)
#define BURNER_PIN GPIO_NUM_36
#define ZONE_1_PIN GPIO_NUM_32
#define ZONE_2_PIN GPIO_NUM_33
#define ZONE_3_PIN GPIO_NUM_34
#define ZONE_4_PIN GPIO_NUM_35
#define ZONE_5_PIN GPIO_NUM_25
#define ZONE_6_PIN GPIO_NUM_26

// LED Configuration (update for your ESP32 board)
#define LED_GPIO_PIN GPIO_NUM_5
#define LED_ON_LEVEL 0     // 0 for active-low, 1 for active-high
#define LED_OFF_LEVEL 1

// Input Signal Configuration
#define INPUT_ACTIVE_LEVEL 1      // 1 for active-high, 0 for active-low
#define DEBOUNCE_TIME_MS 100      // Input debounce time in milliseconds
#define READING_STABLE_COUNT 3    // Required consecutive stable readings

#endif // CONFIG_H